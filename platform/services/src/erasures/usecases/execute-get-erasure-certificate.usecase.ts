/**
 * ExecuteGetErasureCertificate - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: erasures
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetErasureCertificate Use Case
 *
 * DDD: Application use case for get ErasureCertificate.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetErasureCertificateInput, GetErasureCertificateOutput } from "../dto/erasure-certificate.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aliaskeep/services/_shared/index.js";
import type { ErasureCertificateRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetErasureCertificate {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly erasureCertificate: ErasureCertificateRepository
  ) {}

  async execute(input: GetErasureCertificateInput): Promise<GetErasureCertificateOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ersId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).certificateId) {
      throw new ValidationError("certificateId is required");
    }
    const certificateId = (input as any).certificateId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.erasureCertificate.getErasureCertificate({ certificateId: certificateId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${certificateId}`);
    }

    return entity as GetErasureCertificateOutput;
  }
}
