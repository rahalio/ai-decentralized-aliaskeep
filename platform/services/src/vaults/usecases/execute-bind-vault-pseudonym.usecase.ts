/**
 * ExecuteBindVaultPseudonym - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: vaults
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteBindVaultPseudonym Use Case
 *
 * DDD: Application use case for get BindPseudonym.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { BindVaultPseudonymInput, BindVaultPseudonymOutput } from "../dto/bind-pseudonym.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aliaskeep/services/_shared/index.js";
import type { BindPseudonymRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteBindVaultPseudonym {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly bindPseudonym: BindPseudonymRepository
  ) {}

  async execute(input: BindVaultPseudonymInput): Promise<BindVaultPseudonymOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.vltId();

    // All field names extracted from OpenAPI spec: pseudonymId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).vaultId) {
      throw new ValidationError("vaultId is required");
    }
    const vaultId = (input as any).vaultId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.bindPseudonym.bindVaultPseudonym({ vaultId: vaultId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${vaultId}`);
    }

    return entity as BindVaultPseudonymOutput;
  }
}
