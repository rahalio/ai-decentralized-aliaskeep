/**
 * ExecuteFlagVaultDuplicate - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: vaults
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteFlagVaultDuplicate Use Case
 *
 * DDD: Application use case for get FlagDuplicate.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { FlagVaultDuplicateInput, FlagVaultDuplicateOutput } from "../dto/flag-duplicate.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aliaskeep/services/_shared/index.js";
import type { FlagDuplicateRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteFlagVaultDuplicate {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly flagDuplicate: FlagDuplicateRepository
  ) {}

  async execute(input: FlagVaultDuplicateInput): Promise<FlagVaultDuplicateOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.vltId();

    // All field names extracted from OpenAPI spec: reason, relatedVaultId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).vaultId) {
      throw new ValidationError("vaultId is required");
    }
    const vaultId = (input as any).vaultId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.flagDuplicate.flagVaultDuplicate({ vaultId: vaultId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${vaultId}`);
    }

    return entity as FlagVaultDuplicateOutput;
  }
}
