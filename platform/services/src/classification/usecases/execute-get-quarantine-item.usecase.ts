/**
 * ExecuteGetQuarantineItem - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: classification
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetQuarantineItem Use Case
 *
 * DDD: Application use case for get Quarantine.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetQuarantineItemInput, GetQuarantineItemOutput } from "../dto/quarantine.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aliaskeep/services/_shared/index.js";
import type { QuarantineRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetQuarantineItem {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly quarantine: QuarantineRepository
  ) {}

  async execute(input: GetQuarantineItemInput): Promise<GetQuarantineItemOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.clsId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).quarantineId) {
      throw new ValidationError("quarantineId is required");
    }
    const quarantineId = (input as any).quarantineId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.quarantine.getQuarantineItem({ quarantineId: quarantineId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${quarantineId}`);
    }

    return entity as GetQuarantineItemOutput;
  }
}
