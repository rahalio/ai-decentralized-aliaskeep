/**
 * ExecuteWaiveQuarantineItem - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: classification
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteWaiveQuarantineItem Use Case
 *
 * DDD: Application use case for get Waive.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { WaiveQuarantineItemInput, WaiveQuarantineItemOutput } from "../dto/waive.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aliaskeep/services/_shared/index.js";
import type { WaiveRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteWaiveQuarantineItem {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly waive: WaiveRepository
  ) {}

  async execute(input: WaiveQuarantineItemInput): Promise<WaiveQuarantineItemOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.clsId();

    // All field names extracted from OpenAPI spec: note, riskAcceptance
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).quarantineId) {
      throw new ValidationError("quarantineId is required");
    }
    const quarantineId = (input as any).quarantineId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.waive.waiveQuarantineItem({ quarantineId: quarantineId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${quarantineId}`);
    }

    return entity as WaiveQuarantineItemOutput;
  }
}
