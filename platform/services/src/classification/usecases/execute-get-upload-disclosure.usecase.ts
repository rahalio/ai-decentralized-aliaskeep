/**
 * ExecuteGetUploadDisclosure - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: classification
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetUploadDisclosure Use Case
 *
 * DDD: Application use case for get Disclosure.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetUploadDisclosureInput, GetUploadDisclosureOutput } from "../dto/disclosure.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aliaskeep/services/_shared/index.js";
import type { DisclosureRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteGetUploadDisclosure {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly disclosure: DisclosureRepository
  ) {}

  async execute(input: GetUploadDisclosureInput): Promise<GetUploadDisclosureOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.clsId();

    // All field names extracted from OpenAPI spec: none
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.disclosure.getUploadDisclosure({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as GetUploadDisclosureOutput;
  }
}
