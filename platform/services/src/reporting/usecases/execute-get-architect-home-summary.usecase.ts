/**
 * ExecuteGetArchitectHomeSummary - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: reporting
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetArchitectHomeSummary Use Case
 *
 * DDD: Application use case for get HomeSummary.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetArchitectHomeSummaryInput, GetArchitectHomeSummaryOutput } from "../dto/home-summary.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aliaskeep/services/_shared/index.js";
import type { HomeSummaryRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteGetArchitectHomeSummary {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly homeSummary: HomeSummaryRepository
  ) {}

  async execute(input: GetArchitectHomeSummaryInput): Promise<GetArchitectHomeSummaryOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.rptId();

    // All field names extracted from OpenAPI spec: none
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.homeSummary.getArchitectHomeSummary({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as GetArchitectHomeSummaryOutput;
  }
}
