/**
 * ExecuteGetLinkAuditReport - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: reporting
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetLinkAuditReport Use Case
 *
 * DDD: Application use case for get LinkAudit.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetLinkAuditReportInput, GetLinkAuditReportOutput } from "../dto/link-audit.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aliaskeep/services/_shared/index.js";
import type { LinkAuditRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteGetLinkAuditReport {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly linkAudit: LinkAuditRepository
  ) {}

  async execute(input: GetLinkAuditReportInput): Promise<GetLinkAuditReportOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.rptId();

    // All field names extracted from OpenAPI spec: none
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.linkAudit.getLinkAuditReport({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as GetLinkAuditReportOutput;
  }
}
