/**
 * ExecuteRequireFabricIdentityPath - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: fabric
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRequireFabricIdentityPath Use Case
 *
 * DDD: Application use case for get RequirePath.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RequireFabricIdentityPathInput, RequireFabricIdentityPathOutput } from "../dto/require-path.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aliaskeep/services/_shared/index.js";
import type { RequirePathRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteRequireFabricIdentityPath {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly requirePath: RequirePathRepository
  ) {}

  async execute(input: RequireFabricIdentityPathInput): Promise<RequireFabricIdentityPathOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.fabId();

    // All field names extracted from OpenAPI spec: note, requiredPath
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).profileId) {
      throw new ValidationError("profileId is required");
    }
    const profileId = (input as any).profileId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.requirePath.requireFabricIdentityPath({ profileId: profileId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${profileId}`);
    }

    return entity as RequireFabricIdentityPathOutput;
  }
}
