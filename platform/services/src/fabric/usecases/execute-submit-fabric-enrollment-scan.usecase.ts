/**
 * ExecuteSubmitFabricEnrollmentScan - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: fabric
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteSubmitFabricEnrollmentScan Use Case
 *
 * DDD: Application use case for create Profile.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { SubmitFabricEnrollmentScanInput, SubmitFabricEnrollmentScanOutput } from "../dto/profile.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aliaskeep/services/_shared/index.js";
import type { ProfileRepository } from "../ports";
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanCreate } from "../policies/can-create.policy";
export class ExecuteSubmitFabricEnrollmentScan {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly profile: ProfileRepository
  ) {}

  async execute(input: SubmitFabricEnrollmentScanInput): Promise<SubmitFabricEnrollmentScanOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.fabId();

    // All field names extracted from OpenAPI spec: attributes, mspId, participantId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!input.participantId) {
      throw new ValidationError("participantId is required");
    }
    if (!input.attributes) {
      throw new ValidationError("attributes is required");
    }    // Check policy
    if (!CanCreate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Generate entity ID in services layer using IdGeneratorService (fabric domain)
    const entityId = this.idGenerator.fabId();

    // Create entity via repository (pass ID and correlation ID for response meta)
    const entity = await this.profile.submitFabricEnrollmentScan({ ...input, id: entityId, createdByActorId: (input as any).createdByActorId ?? this.context.getUserId(), createdByActorType: (input as any).createdByActorType ?? "human", orgId: this.context.getOrgId(), correlationId } as any);

    return entity as SubmitFabricEnrollmentScanOutput;
  }
}
