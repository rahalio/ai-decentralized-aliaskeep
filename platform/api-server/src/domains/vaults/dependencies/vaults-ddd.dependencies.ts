/**
 * Vaults DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: vaults
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Vaults Domain Module - Composition root
 *
 * DDD: Composition root for vaults domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  BindPseudonymRepositoryAdapter,
  FlagDuplicateRepositoryAdapter,
  VaultRepositoryAdapter,
} from "@aliaskeep/adapters/vaults";
import { getIdGeneratorService } from "@aliaskeep/adapters";
import type { AdapterDynamoDBClient } from "@aliaskeep/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteBindVaultPseudonym,
  ExecuteCreateIdentityVault,
  ExecuteFlagVaultDuplicate,
  ExecuteGetIdentityVault,
  ExecuteListIdentityVaults,
} from "@aliaskeep/services/vaults/usecases";
// Ports (interfaces)
import type {
  BindPseudonymRepository,
  FlagDuplicateRepository,
  VaultRepository,
} from "@aliaskeep/services/vaults/ports";

export interface VaultsDomainModule {
  repos: {
    bindPseudonyms: BindPseudonymRepository;
    flagDuplicates: FlagDuplicateRepository;
    vaults: VaultRepository;
  };
  useCases: {
    bindPseudonyms: {
      get: ExecuteBindVaultPseudonym;
    };
    flagDuplicates: {
      get: ExecuteFlagVaultDuplicate;
    };
    vaults: {
      create: ExecuteCreateIdentityVault;
      get: ExecuteGetIdentityVault;
      list: ExecuteListIdentityVaults;
    };
  };
}

export function buildVaultsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): VaultsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      bindPseudonyms?: BindPseudonymRepository;
      flagDuplicates?: FlagDuplicateRepository;
      vaults?: VaultRepository;
    } = {};

    obj.bindPseudonyms = new BindPseudonymRepositoryAdapter(dynamoClient);
    obj.flagDuplicates = new FlagDuplicateRepositoryAdapter(dynamoClient);
    obj.vaults = new VaultRepositoryAdapter(dynamoClient);
    return {
      bindPseudonyms: obj.bindPseudonyms!,
      flagDuplicates: obj.flagDuplicates!,
      vaults: obj.vaults!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    bindPseudonyms: {
      get: new ExecuteBindVaultPseudonym(executionContext, idGenerator, repos.bindPseudonyms),
    },
    flagDuplicates: {
      get: new ExecuteFlagVaultDuplicate(executionContext, idGenerator, repos.flagDuplicates),
    },
    vaults: {
      create: new ExecuteCreateIdentityVault(executionContext, idGenerator, repos.vaults),
      get: new ExecuteGetIdentityVault(executionContext, idGenerator, repos.vaults),
      list: new ExecuteListIdentityVaults(executionContext, idGenerator, repos.vaults),
    },
  };
  return { repos, useCases: useCases as VaultsDomainModule["useCases"] };
}
