/**
 * Erasures DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: erasures
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Erasures Domain Module - Composition root
 *
 * DDD: Composition root for erasures domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  ErasureCertificateRepositoryAdapter,
  ErasureRepositoryAdapter,
  NudgeRepositoryAdapter,
  PropagationRepositoryAdapter,
} from "@aliaskeep/adapters/erasures";
import { getIdGeneratorService } from "@aliaskeep/adapters";
import type { AdapterDynamoDBClient } from "@aliaskeep/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteExecuteLogicalErasure,
  ExecuteGetErasureCertificate,
  ExecuteGetErasurePropagation,
  ExecuteGetLogicalErasure,
  ExecuteListLogicalErasures,
  ExecuteNudgeErasurePropagation,
} from "@aliaskeep/services/erasures/usecases";
// Ports (interfaces)
import type {
  ErasureCertificateRepository,
  ErasureRepository,
  NudgeRepository,
  PropagationRepository,
} from "@aliaskeep/services/erasures/ports";

export interface ErasuresDomainModule {
  repos: {
    erasureCertificates: ErasureCertificateRepository;
    erasures: ErasureRepository;
    nudges: NudgeRepository;
    propagations: PropagationRepository;
  };
  useCases: {
    erasureCertificates: {
      get: ExecuteGetErasureCertificate;
    };
    erasures: {
      create: ExecuteExecuteLogicalErasure;
      get: ExecuteGetLogicalErasure;
      list: ExecuteListLogicalErasures;
    };
    nudges: {
      get: ExecuteNudgeErasurePropagation;
    };
    propagations: {
      get: ExecuteGetErasurePropagation;
    };
  };
}

export function buildErasuresDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): ErasuresDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      erasureCertificates?: ErasureCertificateRepository;
      erasures?: ErasureRepository;
      nudges?: NudgeRepository;
      propagations?: PropagationRepository;
    } = {};

    obj.erasureCertificates = new ErasureCertificateRepositoryAdapter(dynamoClient);
    obj.erasures = new ErasureRepositoryAdapter(dynamoClient);
    obj.nudges = new NudgeRepositoryAdapter(dynamoClient);
    obj.propagations = new PropagationRepositoryAdapter(dynamoClient);
    return {
      erasureCertificates: obj.erasureCertificates!,
      erasures: obj.erasures!,
      nudges: obj.nudges!,
      propagations: obj.propagations!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    erasureCertificates: {
      get: new ExecuteGetErasureCertificate(executionContext, idGenerator, repos.erasureCertificates),
    },
    erasures: {
      create: new ExecuteExecuteLogicalErasure(executionContext, idGenerator, repos.erasures),
      get: new ExecuteGetLogicalErasure(executionContext, idGenerator, repos.erasures),
      list: new ExecuteListLogicalErasures(executionContext, idGenerator, repos.erasures),
    },
    nudges: {
      get: new ExecuteNudgeErasurePropagation(executionContext, idGenerator, repos.nudges),
    },
    propagations: {
      get: new ExecuteGetErasurePropagation(executionContext, idGenerator, repos.propagations),
    },
  };
  return { repos, useCases: useCases as ErasuresDomainModule["useCases"] };
}
