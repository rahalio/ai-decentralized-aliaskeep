/**
 * Links DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: links
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Links Domain Module - Composition root
 *
 * DDD: Composition root for links domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  ApproveRepositoryAdapter,
  GrantRepositoryAdapter,
  PolicyRepositoryAdapter,
  RejectRepositoryAdapter,
  RestoreRepositoryAdapter,
  RevokeRepositoryAdapter,
  SuspendRepositoryAdapter,
} from "@aliaskeep/adapters/links";
import { getIdGeneratorService } from "@aliaskeep/adapters";
import type { AdapterDynamoDBClient } from "@aliaskeep/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteAllocateLinkGrant,
  ExecuteApproveLinkPolicy,
  ExecuteCreateLinkPolicy,
  ExecuteGetLinkGrant,
  ExecuteListLinkGrants,
  ExecuteListLinkPolicies,
  ExecuteRejectLinkPolicy,
  ExecuteRestoreLinkGrant,
  ExecuteRevokeLinkGrant,
  ExecuteSuspendLinkGrant,
} from "@aliaskeep/services/links/usecases";
// Ports (interfaces)
import type {
  ApproveRepository,
  GrantRepository,
  PolicyRepository,
  RejectRepository,
  RestoreRepository,
  RevokeRepository,
  SuspendRepository,
} from "@aliaskeep/services/links/ports";

export interface LinksDomainModule {
  repos: {
    approves: ApproveRepository;
    grants: GrantRepository;
    policies: PolicyRepository;
    rejects: RejectRepository;
    restores: RestoreRepository;
    revokes: RevokeRepository;
    suspends: SuspendRepository;
  };
  useCases: {
    approves: {
      create: ExecuteApproveLinkPolicy;
    };
    grants: {
      get: ExecuteGetLinkGrant;
      list: ExecuteListLinkGrants;
    };
    policies: {
      create: ExecuteCreateLinkPolicy;
      list: ExecuteListLinkPolicies;
    };
    rejects: {
      create: ExecuteRejectLinkPolicy;
    };
    restores: {
      get: ExecuteRestoreLinkGrant;
    };
    revokes: {
      get: ExecuteRevokeLinkGrant;
    };
    suspends: {
      get: ExecuteSuspendLinkGrant;
    };
  };
}

export function buildLinksDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): LinksDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      approves?: ApproveRepository;
      grants?: GrantRepository;
      policies?: PolicyRepository;
      rejects?: RejectRepository;
      restores?: RestoreRepository;
      revokes?: RevokeRepository;
      suspends?: SuspendRepository;
    } = {};

    obj.approves = new ApproveRepositoryAdapter(dynamoClient);
    obj.grants = new GrantRepositoryAdapter(dynamoClient);
    obj.policies = new PolicyRepositoryAdapter(dynamoClient);
    obj.rejects = new RejectRepositoryAdapter(dynamoClient);
    obj.restores = new RestoreRepositoryAdapter(dynamoClient);
    obj.revokes = new RevokeRepositoryAdapter(dynamoClient);
    obj.suspends = new SuspendRepositoryAdapter(dynamoClient);
    return {
      approves: obj.approves!,
      grants: obj.grants!,
      policies: obj.policies!,
      rejects: obj.rejects!,
      restores: obj.restores!,
      revokes: obj.revokes!,
      suspends: obj.suspends!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    approves: {
      create: new ExecuteApproveLinkPolicy(executionContext, idGenerator, repos.approves),
    },
    grants: {
      get: new ExecuteGetLinkGrant(executionContext, idGenerator, repos.grants),
      list: new ExecuteListLinkGrants(executionContext, idGenerator, repos.grants),
    },
    policies: {
      create: new ExecuteCreateLinkPolicy(executionContext, idGenerator, repos.policies),
      list: new ExecuteListLinkPolicies(executionContext, idGenerator, repos.policies),
    },
    rejects: {
      create: new ExecuteRejectLinkPolicy(executionContext, idGenerator, repos.rejects),
    },
    restores: {
      get: new ExecuteRestoreLinkGrant(executionContext, idGenerator, repos.restores),
    },
    revokes: {
      get: new ExecuteRevokeLinkGrant(executionContext, idGenerator, repos.revokes),
    },
    suspends: {
      get: new ExecuteSuspendLinkGrant(executionContext, idGenerator, repos.suspends),
    },
  };
  return { repos, useCases: useCases as LinksDomainModule["useCases"] };
}
