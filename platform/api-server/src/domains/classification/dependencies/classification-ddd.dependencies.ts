/**
 * Classification DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: classification
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Classification Domain Module - Composition root
 *
 * DDD: Composition root for classification domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  DisclosureRepositoryAdapter,
  QuarantineRepositoryAdapter,
  RejectRepositoryAdapter,
  RemediateRepositoryAdapter,
  ScanRepositoryAdapter,
  WaiveRepositoryAdapter,
} from "@aliaskeep/adapters/classification";
import { getIdGeneratorService } from "@aliaskeep/adapters";
import type { AdapterDynamoDBClient } from "@aliaskeep/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteGetQuarantineItem,
  ExecuteGetUploadDisclosure,
  ExecuteListQuarantineItems,
  ExecutePublishUploadDisclosure,
  ExecuteRejectQuarantineItem,
  ExecuteRemediateQuarantineItem,
  ExecuteSubmitClassificationScan,
  ExecuteWaiveQuarantineItem,
} from "@aliaskeep/services/classification/usecases";
// Ports (interfaces)
import type {
  DisclosureRepository,
  QuarantineRepository,
  RejectRepository,
  RemediateRepository,
  ScanRepository,
  WaiveRepository,
} from "@aliaskeep/services/classification/ports";

export interface ClassificationDomainModule {
  repos: {
    disclosures: DisclosureRepository;
    quarantines: QuarantineRepository;
    rejects: RejectRepository;
    remediates: RemediateRepository;
    scans: ScanRepository;
    waives: WaiveRepository;
  };
  useCases: {
    disclosures: {
      get: ExecutePublishUploadDisclosure;
    };
    quarantines: {
      get: ExecuteGetQuarantineItem;
      list: ExecuteListQuarantineItems;
    };
    rejects: {
      create: ExecuteRejectQuarantineItem;
    };
    remediates: {
      get: ExecuteRemediateQuarantineItem;
    };
    scans: {
      create: ExecuteSubmitClassificationScan;
    };
    waives: {
      get: ExecuteWaiveQuarantineItem;
    };
  };
}

export function buildClassificationDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): ClassificationDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      disclosures?: DisclosureRepository;
      quarantines?: QuarantineRepository;
      rejects?: RejectRepository;
      remediates?: RemediateRepository;
      scans?: ScanRepository;
      waives?: WaiveRepository;
    } = {};

    obj.disclosures = new DisclosureRepositoryAdapter(dynamoClient);
    obj.quarantines = new QuarantineRepositoryAdapter(dynamoClient);
    obj.rejects = new RejectRepositoryAdapter(dynamoClient);
    obj.remediates = new RemediateRepositoryAdapter(dynamoClient);
    obj.scans = new ScanRepositoryAdapter(dynamoClient);
    obj.waives = new WaiveRepositoryAdapter(dynamoClient);
    return {
      disclosures: obj.disclosures!,
      quarantines: obj.quarantines!,
      rejects: obj.rejects!,
      remediates: obj.remediates!,
      scans: obj.scans!,
      waives: obj.waives!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    disclosures: {
      get: new ExecutePublishUploadDisclosure(executionContext, idGenerator, repos.disclosures),
    },
    quarantines: {
      get: new ExecuteGetQuarantineItem(executionContext, idGenerator, repos.quarantines),
      list: new ExecuteListQuarantineItems(executionContext, idGenerator, repos.quarantines),
    },
    rejects: {
      create: new ExecuteRejectQuarantineItem(executionContext, idGenerator, repos.rejects),
    },
    remediates: {
      get: new ExecuteRemediateQuarantineItem(executionContext, idGenerator, repos.remediates),
    },
    scans: {
      create: new ExecuteSubmitClassificationScan(executionContext, idGenerator, repos.scans),
    },
    waives: {
      get: new ExecuteWaiveQuarantineItem(executionContext, idGenerator, repos.waives),
    },
  };
  return { repos, useCases: useCases as ClassificationDomainModule["useCases"] };
}
