/**
 * GetIdentityVault handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: vaults
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getIdentityVault } from "@aliaskeep/api-server/domains/vaults/handlers";
import type { VaultsDDDDependencies } from "@aliaskeep/api-server/domains/vaults";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@aliaskeep/tests/shared";
import { VAULTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetIdentityVault", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: VaultsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<VaultsDDDDependencies>(
      VAULTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetIdentityVault, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { vaultId: entity.id };

    await getIdentityVault(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetIdentityVault.execute).toHaveBeenCalledWith(expect.objectContaining({ vaultId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { vaultId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetIdentityVault, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getIdentityVault(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
